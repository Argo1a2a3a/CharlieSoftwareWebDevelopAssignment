CREATE TABLE Movies(ID INTEGER PRIMARY KEY AUTOINCREMENT,
                    Name TEXT NOT NULL,
                    Genre TEXT NOT NULL,
                    Year INTEGER NOT NULL,
                    Summary TEXT NOT NULL);